<Window x:Class="ASCOMTester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ASCOMTester"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        DataContext="{Binding Main, Source={StaticResource Locator}}"
        >
    <Grid>
        <StackPanel>
            <Button Command="{Binding ChooseCommand}" Content="Choose" Width="80" Margin="10"/>
            <Button Command="{Binding ConnectCommand}" Content="Connect" Width="80"/>

        </StackPanel>
    </Grid>
</Window>
